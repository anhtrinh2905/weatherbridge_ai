---
scope: WeatherBridge AI competition MVP, pilot evolution, and product runtime boundaries
purpose: Build substrate for the development team and an architecture proof for VAIC judges
altitude: initiative
updated: 2026-07-17T18:06
---

- (event) Architecture create run activated from the VAIC prompt, PRD package, brownfield repository audit, and Fast-path inference.
- (decision) Binds deliverables; prevents an architecture that serves only judges or only builders; produce a build-substrate spine plus a human-readable AI-first development roadmap for the team and VAIC judges.
- (constraint) Existing adopted boundaries: modular monorepo with independently deployable fe, be, worker, ai, infra; online AI belongs in be/src/ai, offline evaluation/training in ai, and worker remains independently deployable.
- (constraint) PRD safety invariant: deterministic rules own hazard detection, severity, scope, and deadline; LLM may phrase but may not alter them; evacuation-class alerts require an authorized human approval gate.
- (decision) Binds all capabilities; prevents chatbot-first and end-to-end opaque warning implementations; adopt Safety-first Event-driven Decision Intelligence with deterministic control, bounded AI transforms, and human authority for life-critical release.
- (decision) Binds FR-1, FR-2, FR-7, FR-11, FR-12; prevents provider-specific schemas and untraceable numbers; every source adapter emits an immutable, versioned ForecastSnapshot carrying location, horizon, values, units, source, model/run time, fetched time, freshness, and exercise flag.
- (decision) Binds FR-3, FR-4, FR-6; prevents LLM-controlled safety decisions and threshold drift; a deterministic Risk Engine emits a versioned RiskAssessment with matched threshold, evidence values, severity, affected villages, phenomenon time, action lead time, deadline, and dedupe key.
- (decision) Binds FR-4 and evacuation warnings; prevents software from issuing unauthorized evacuation orders; evacuation-class RiskAssessments create PendingApproval, and only an authorized commune official can transition them to Released or Rejected while preparation alerts may release automatically.
- (decision) Binds FR-5, FR-8, FR-9, FR-20; prevents invented safety advice; recommended actions and evacuation destinations come from a curated, versioned ActionProtocol keyed by hazard, severity, occupation, and locality, never from model memory.
- (decision) Binds FR-5 and FR-24; prevents free-form AI output and silent hallucination; the Action Bulletin Composer receives only approved evidence and protocols, returns a typed four-part ActionBulletin, then deterministic validators check schema, copied facts, severity, deadline, action allowlist, length, and language before release; any failure or 10-second timeout uses a deterministic template.
- (decision) Binds FR-15 and FR-16; prevents unreviewed machine translation in life-critical content; runtime local-language output renders human-reviewed templates with variables, Thai TTS runs asynchronously and caches audio, Hmong uses consented recordings for the MVP, and AI translation is authoring-time only until native-speaker approval exists.
- (decision) Binds ingest, composition, delivery, escalation, and audio work; prevents lost jobs and duplicate side effects; state changes commit a domain record and outbox event atomically in PostgreSQL, workers consume via Redis Streams consumer groups with explicit acknowledgement, and every handler is idempotent by event ID plus operation key.
- (decision) Binds shared state; prevents two services owning the same entity; PostgreSQL is the system of record, Redis holds only cache and recoverable transport, object storage holds generated audio/reports, backend application modules exclusively mutate domain entities, and workers invoke those services rather than duplicating table contracts.
- (decision) Binds FR-14 to FR-22; prevents channel failures from changing warning truth; a released Alert is immutable input to independent channel adapters, each DeliveryAttempt is recorded separately, acknowledgements and escalations append events, and offline officer actions preserve device timestamps then sync with conflict-safe idempotency.
- (decision) Binds FR-24 and every AI change; prevents prompt/provider changes shipping without evidence; a versioned task bundle joins input/output schema, prompt, model/provider, retrieval or protocol version, validators, golden dataset, thresholds, and release result; deterministic checks are mandatory and LLM-as-judge is advisory, never the sole gate.
- (decision) Binds identity and vulnerable-household data; prevents demo shortcuts becoming production access control; Keycloak authenticates officials and admins, backend authorization scopes every operation by role and locality, resident devices use revocable household/device credentials, demo data is synthetic, and production collection requires purpose, consent or legal basis, retention, deletion, and access audit.
- (decision) Binds reliability and AI evidence; prevents black-box incidents; one trace ID follows source fetch through assessment, composition, approval, delivery, acknowledgement, and escalation, while telemetry records source age, queue age, validator result, fallback, prompt/model/protocol versions, latency, token cost, delivery outcome, and privacy-safe error detail.
- (decision) Binds runtime topology; prevents training or provider calls blocking HTTP; API serves commands and queries, worker executes scheduled and long-running tasks, ai remains offline-only, migrations run as a release step, environment secrets are external, and production must fail startup if configured to use a mock AI provider.
- (direction) MVP vertical slice: scenario or Open-Meteo data for five seeded locations -> normalization -> frost or cold threshold -> assessment -> structured Vietnamese bulletin -> validation/fallback -> resident card and in-app delivery -> acknowledgement -> append-only evidence; add human relay and local audio only after this slice passes end to end.
- (direction) Pilot evolution: validate official data and thresholds, add station/history adapters, offline-first officer workflow, SMS/Zalo/loudspeaker adapters, production privacy/security, quality and drift monitoring, then calibrated local models or threshold learning only after labeled outcomes exist.
- (assumption) Blocked external facts remain open: format and rights for the Dien Bien station and historical disaster data, authoritative threshold owner, authorized evacuation approver, local-language reviewers and recordings, Zalo/SMS/loudspeaker access, and production hosting target.
- (version) Brownfield seed verified in repository locks/images: Python 3.12, FastAPI 0.128.8, SQLAlchemy 2.0.51, redis-py 6.4.0, React 19.2.7, Vite 8.1.4, PostgreSQL 16, Redis server 7, Keycloak 26.5.2, LiteLLM gateway image 1.81.13-stable.
- (version) External fit verified 2026-07-17: Open-Meteo Forecast API supports seven days by default, up to sixteen days, per-coordinate elevation with 90m DEM downscaling, model selection, and CC BY 4.0 attribution; OASIS CAP 1.2 remains the published standard; Redis 7 supports Streams consumer groups, XACK, pending inspection, and stale-message claiming.
- (decision) Binds FR-2; prevents double altitude correction; downscaling is source-aware and versioned: use provider elevation downscaling when requested, otherwise apply one documented local lapse-rate transform, never both to the same value.
- (decision) Binds FR-7 and every outbound channel; prevents exercises reaching real recipients or contaminating real evidence; exercise mode follows the production pipeline but the exercise flag is immutable, visible in every artifact, restricted to allowlisted destinations, and partitioned in reports and metrics.
- (decision) Binds source outage behavior; prevents stale or missing data being interpreted as safety; no valid fresh snapshot means no new automatic assessment, the last valid forecast remains visibly stale, source failure raises an operational alert, and the UI must never replace unknown state with safe state.
- (constraint) MVP evidence supports frost, cold, fog, and heavy-rain threshold risks; flash-flood and landslide flows are labeled exercise or risk-indicator scenarios until authoritative local hazard maps, thresholds, and station/history data are validated.
- (decision) Binds action retrieval; prevents semantically similar but unsafe guidance; MVP retrieval is deterministic relational lookup over approved ActionProtocols, not vector similarity; semantic retrieval may be evaluated later for non-life-critical explanatory content.
- (decision) Binds service topology; prevents premature distributed-system overhead; retain the modular monorepo and API plus worker deploy units rather than splitting weather, alerts, delivery, and AI into microservices during the competition or initial pilot.
- (decision) Binds MVP AI scope; prevents unsupported model claims and GPU work from displacing the vertical slice; do not train a forecasting model in the competition MVP: consume numerical forecasts, use bounded generative composition and TTS, and collect provenance plus outcomes for later calibration or training.
- (decision) Binds competition execution; prevents pilot hardening displacing eligibility; define three profiles: Submission Core is non-droppable and must prove the exact VAIC minimum, Submission Differentiator starts only after Core passes end to end, and Pilot applies the full safety/reliability contract.
- (decision) Binds source selection; prevents multi-source marketing without arbitration; Submission uses Open-Meteo as primary plus exercise fixtures, with OWM and station adapters inactive until a deterministic approved SourcePolicy governs freshness, quality, failover, disagreement, and switchback.
- (decision) Binds AI grounding; prevents brittle prose parsing; AI returns only bounded non-critical language and immutable fact/action references, while a deterministic renderer inserts values, severity, deadline, place, actions, and destination before final-artifact validation.
- (decision) Binds AI release management; prevents mutable aliases and unsafe rollback; AiTaskRelease is content-addressed with draft-evaluated-approved-active-retired/revoked lifecycle, atomic environment activation, in-flight version pinning, and rollback independent from threshold/action protocol activation.
- (decision) Binds alerts; prevents stale or false warnings remaining authoritative; alerts are immutable revisions with validity windows and supersession links, pending approval expires on stale evidence, and correction, update, retraction, expiry, and all-clear revisions propagate to every channel and accountability view.
- (decision) Binds external channels; prevents impossible exactly-once promises; PostgreSQL state transitions are atomic and internal handlers idempotent, transport is at least once, and each provider declares idempotency support, bounded retry, ambiguous outcome, reconciliation, and severity-specific duplicate-versus-omission policy.
- (decision) Binds release fan-out; prevents mutable profile drift; release freezes affected villages, recipient/cohort membership, language, bulletin/protocol revision, destination, and channel intent, and channel adapters receive frozen DeliveryCommands rather than re-deriving audience or safety content.
- (decision) Binds offline accountability; prevents clock manipulation and deadline races; server time and transactional compare-and-set own workflow state, device time remains labelled evidence with clock-quality metadata, late acknowledgements never rewrite emitted escalations, and NotFound escalates immediately.
- (decision) Binds public UX; prevents API and frontend severity drift; public levels are PREPARE and GO_NOW, with action-first card then evidence, safe distinct from unknown/stale, maximum two action lines at 360px, source/freshness disclosure, and red sound only for GO_NOW.
- (decision) Binds AI-first proof; prevents AI-native claims resting on model presence; release evaluation compares bounded AI against deterministic template baseline and requires measurable clarity, comprehension, or occupation-fit improvement with zero deterministic safety regression.
- (decision) Binds rubric tracking; prevents projected work being presented as achieved; maintain an evidence-based VAIC scorecard with current, Submission Core, and Stretch Target scores, award no points without linked artifacts/tests/demo evidence, and record caps for missing end-to-end flow, unvalidated business assumptions, safety gaps, and licence risk.
- (assumption) Competition priority resolves the PRD tension by treating Web Push as Submission Core because it is in the formal Must tier and local-language TTS as Submission Differentiator; revisit only on an explicit team-owner decision.
- (change) Applied reviewer findings: added binding Submission Core/Differentiator/Pilot profiles, exact VAIC acceptance gate, source/policy semantics, strict contracts, anchored AI rendering, alert revision/frozen fan-out, truthful external-effect semantics, human-relay time authority, role/scope matrix, localization final-artifact validation, fitness gates, and profile control matrix.
- (event) Created VAIC-SELF-ASSESSMENT.md with evidence-only scoring: current baseline 31/100, Submission Core target 72/100, and Differentiator target 82/100. Targets are explicitly conditional, not claimed results.
- (event) Reviewer gate status: deterministic spine lint passed with zero findings and git diff check passed. Existing rubric, PRD-reconciliation, AI-integrity, and divergence findings were applied. Current-technology and safety subagent reviews were cancelled at user direction; web-verified source facts and unresolved safety/technology items remain explicitly represented in the spine Deferred section.
- (event) spine finalized
